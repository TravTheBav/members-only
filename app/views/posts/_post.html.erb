<div id="<%= dom_id post %>">
  <p>
    <strong><%= post.title %></strong>
  </p>

  <p>
    <% if params[:action] == "index" && post.body.length >= 60 %>
      <%= post.body[(0..60)] + "..." %>
    <% else %>
      <%= post.body %>
    <% end %>
  </p>

</div>
